# Proxy all /api/* requests to backend API server
# The :splat captures everything after /api/ and forwards it as-is
/api/*  https://api.safegoglobal.com/:splat  200!

# SPA fallback - serve index.html for all other routes
/*  /index.html  200
