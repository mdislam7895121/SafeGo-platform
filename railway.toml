[build]
builder = "NIXPACKS"
buildCommand = "npm run build"

[deploy]
startCommand = "node --max-old-space-size=2048 dist/index.js"
healthcheckPath = "/api/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
